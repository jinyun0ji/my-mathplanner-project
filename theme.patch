diff --git a/firestore.rules b/firestore.rules
index d70d0f2302f4a7430291210f42638c62e95a88fc..274327076a739a94d24933f1eda06ec0f3cf8599 100644
--- a/firestore.rules
+++ b/firestore.rules
@@ -318,71 +318,79 @@ service cloud.firestore {
           || myProfileDocId() in data.participantIds
         );
     }
 
     function myLinkedUserAuthAliases() {
       return userDoc() != null
         ? [
             userDoc().data.authUid,
             userDoc().data.userUid,
             userDoc().data.uid,
             userDoc().data.studentAuthUid,
             userDoc().data.parentUid
           ]
         : [];
     }
 
     function hasMyLinkedUserAuthAlias(value) {
       return value is string
         && (
           value == uid()
           || value == myProfileDocId()
           || value in myLinkedUserAuthAliases()
         );
     }
 
+    function isMyAuthOrProfileAlias(value) {
+      return value is string && hasMyLinkedUserAuthAlias(value);
+    }
+
     function canReadChatRoomViaStudentAliases(data) {
       return isStudent()
         && (
           (('studentId' in data) && data.studentId == myProfileDocId())
           || (
             ('studentId' in data)
             && data.studentId is string
             && exists(/databases/$(database)/documents/users/$(data.studentId))
             && get(/databases/$(database)/documents/users/$(data.studentId)).data.authUid == uid()
           )
           || (('studentUid' in data) && hasMyLinkedUserAuthAlias(data.studentUid))
           || (('studentAuthUid' in data) && hasMyLinkedUserAuthAlias(data.studentAuthUid))
         );
     }
 
     function canReadChatRoomViaParentAliases(data) {
       return isParent()
         && (
           (('parentId' in data) && data.parentId == myProfileDocId())
+          || (('parentId' in data) && data.parentId == uid())
+          || (('parentUid' in data) && data.parentUid == myProfileDocId())
           || (('parentUid' in data) && hasMyLinkedUserAuthAlias(data.parentUid))
+          || (data.participantIds is list && myProfileDocId() in data.participantIds)
           || (('studentId' in data) && data.studentId in parentStudentUids())
+          || (('studentId' in data) && data.studentId in parentStudentIds())
         );
     }
 
     function canReadChatRoom(data) {
       return isSignedIn()
         && data != null
         && (
           isStaffOrAdminOrTeacher()
           || isChatRoomParticipant(data)
           || (data.participantIds is list && myProfileDocId() in data.participantIds)
           || canReadChatRoomViaStudentAliases(data)
           || canReadChatRoomViaParentAliases(data)
         );
     }
 
     function canAccessChatRoomData(data) {
       return canReadChatRoom(data);
     }
 
     function canAccessChatRoomDoc() {
       return canReadChatRoom(resource.data);
     }
 
 
     function announcementAudienceAuthUids() {
@@ -448,108 +456,109 @@ service cloud.firestore {
         && request.resource.data.createdBy == uid()
         && request.resource.data.updatedBy == uid()
         && request.resource.data.roomType in ['parent_teacher', 'parent_institute']
         && hasValidChatRoomTypeForSlot();
     }
 
 
     function isValidStudentDirectChatRoomCreate() {
       return isStudent()
         && request.resource.data.type == 'individual'
         && request.resource.data.participantIds is list
         && request.resource.data.participantIds.size() == 2
         && uid() in request.resource.data.participantIds
         && hasAllowedDirectChatCounterpart(request.resource.data.participantIds)
         && request.resource.data.internalOnly == true
         && request.resource.data.studentId == myProfileDocId()
         && request.resource.data.studentUid == uid()
         && request.resource.data.studentAuthUid == uid()
         && request.resource.data.createdBy == uid()
         && request.resource.data.updatedBy == uid()
         && request.resource.data.roomType in ['student_teacher', 'student_institute']
         && hasValidChatRoomTypeForSlot();
     }
 
     function isAllowedChatRoomUpdateFieldSet() {
-      return request.resource.data.diff(resource.data).changedKeys()
-        .hasOnly(['updatedAt', 'updatedBy', 'lastMessageText', 'lastMessageAt', 'lastMessageSenderId', 'unreadCountByUser', 'status']);
+      return request.resource.data.diff(resource.data).affectedKeys()
+        .hasOnly(['updatedAt', 'updatedBy', 'lastMessageText', 'lastMessageAt', 'lastMessageSenderId', 'lastSenderId']);
     }
 
     function isValidDirectChatRoomPatch() {
       return isAllowedChatRoomUpdateFieldSet()
-        && request.resource.data.updatedBy == uid()
-        && request.resource.data.lastMessageSenderId == uid();
+        && (!('updatedBy' in request.resource.data) || isMyAuthOrProfileAlias(request.resource.data.updatedBy))
+        && (!('lastMessageSenderId' in request.resource.data) || isMyAuthOrProfileAlias(request.resource.data.lastMessageSenderId))
+        && (!('lastSenderId' in request.resource.data) || isMyAuthOrProfileAlias(request.resource.data.lastSenderId));
     }
 
     function isAllowedChatMessageCreateFields() {
       return request.resource.data.keys().hasOnly([
         'roomId',
         'senderId',
         'senderRole',
         'senderName',
         'messageType',
         'text',
         'attachments',
         'createdAt',
         'localCreatedAtMs',
         'internalOnly',
         'clientTempId',
         'readBy',
         'isBroadcastCopy',
         'broadcastId',
         'editedAt',
         'deletedAt'
       ]);
     }
 
 
 
     function isValidChatAttachmentList() {
       return request.resource.data.attachments is list
         && request.resource.data.attachments.size() <= 1
         && (
           request.resource.data.attachments.size() == 0
           || (
             request.resource.data.attachments[0].keys().hasOnly(['type', 'name', 'url', 'path', 'size', 'contentType'])
             && request.resource.data.attachments[0].type in ['image', 'pdf']
             && request.resource.data.attachments[0].name is string
             && request.resource.data.attachments[0].url is string
             && request.resource.data.attachments[0].path is string
             && request.resource.data.attachments[0].size is int
             && request.resource.data.attachments[0].contentType in ['image/jpeg', 'image/png', 'image/webp', 'application/pdf']
           )
         );
     }
 
     function hasMessageTextOrAttachment() {
       return request.resource.data.text.size() > 0 || request.resource.data.attachments.size() > 0;
     }
 
     function isValidDirectChatMessageCreate(roomId) {
       return canAccessChatRoomById(roomId)
         && request.resource.data.roomId == roomId
-        && request.resource.data.senderId == uid()
+        && isMyAuthOrProfileAlias(request.resource.data.senderId)
         && request.resource.data.messageType in ['text', 'image', 'file']
         && request.resource.data.text is string
         && request.resource.data.text.size() <= 4000
         && isValidChatAttachmentList()
         && hasMessageTextOrAttachment()
         && request.resource.data.internalOnly == true
         && (!('senderRole' in request.resource.data) || request.resource.data.senderRole is string || request.resource.data.senderRole == null)
         && (!('senderName' in request.resource.data) || request.resource.data.senderName is string || request.resource.data.senderName == null)
         && (!('clientTempId' in request.resource.data) || request.resource.data.clientTempId is string || request.resource.data.clientTempId == null)
         && (!('localCreatedAtMs' in request.resource.data) || request.resource.data.localCreatedAtMs is int || request.resource.data.localCreatedAtMs is float || request.resource.data.localCreatedAtMs == null)
         && (!('isBroadcastCopy' in request.resource.data) || request.resource.data.isBroadcastCopy == false || request.resource.data.isBroadcastCopy == null)
         && (!('broadcastId' in request.resource.data) || request.resource.data.broadcastId == null);
     }
 
 
     function isAccountDeletionUpdate(userId) {
       return signedIn()
         && userId == myProfileDocId()
         && request.resource.data.diff(resource.data).affectedKeys().hasOnly([
           'status',
           'deletionRequested',
           'deletionRequestedAt',
           'deletionRequestedBy',
           'active',
           'updatedAt'
@@ -844,51 +853,51 @@ service cloud.firestore {
       allow update: if isSignedIn()
         && (
           isStaffOrAdminOrTeacher()
           || (
             canReadChatRoom(resource.data)
             && isValidDirectChatRoomPatch()
           )
         );
 
       allow delete: if isStaffOrAdminOrTeacher();
     }
 
     match /chatRooms/{roomId}/messages/{messageId} {
       function roomDoc() {
         return get(/databases/$(database)/documents/chatRooms/$(roomId));
       }
 
       function canReadRoomMessages() {
         return exists(/databases/$(database)/documents/chatRooms/$(roomId))
           && canReadChatRoom(roomDoc().data);
       }
 
       allow read: if canReadRoomMessages();
 
       allow create: if canReadRoomMessages()
-        && request.resource.data.senderId == uid()
+        && isMyAuthOrProfileAlias(request.resource.data.senderId)
         && request.resource.data.messageType in ['text', 'image', 'file']
         && request.resource.data.text is string
         && request.resource.data.text.size() <= 4000
         && isValidChatAttachmentList()
         && hasMessageTextOrAttachment()
         && request.resource.data.internalOnly == true;
 
       allow update: if isStaffOrAdminOrTeacher();
       allow delete: if false;
     }
 
     match /chatBroadcastLogs/{broadcastId} {
       allow read, write: if isStaffOrAdminOrTeacher();
     }
 
     match /chats/{roomId} {
       function isChatParticipant() {
         return isSignedIn()
           && resource.data.participantIds is list
           && uid() in resource.data.participantIds;
       }
 
       allow get, list: if isStaffOrAdminOrTeacher() || isChatParticipant();
 
       allow create: if isSignedIn();

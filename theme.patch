diff --git a/firestore.rules b/firestore.rules
index 2b0e0bcbaa588d59c592a226eb21a0222d0d40e7..5606ca039b817cc21010055f0033328eefdfdc7f 100644
--- a/firestore.rules
+++ b/firestore.rules
@@ -287,63 +287,78 @@ service cloud.firestore {
       );
     }
 
     function canAccessLegacyChatData(data) {
       return isSignedIn() && data != null && (
         isStaffOrAdminOrTeacher()
         || isLegacyChatParticipantData(data)
         || parentHasStudentAccess(data.studentId)
       );
     }
 
     function myParticipantKeys() {
       return [
         uid(),
         myProfileDocId()
       ];
     }
 
     function hasMyParticipantKey(listField) {
       return listField is list && (
         uid() in listField
         || myProfileDocId() in listField
       );
     }
 
-    function canAccessChatRoomData(data) {
+    function isChatRoomParticipant(data) {
+      return isSignedIn()
+        && data != null
+        && data.participantIds is list
+        && (
+          uid() in data.participantIds
+          || myProfileDocId() in data.participantIds
+        );
+    }
+
+    function canReadChatRoom(data) {
       return isSignedIn()
         && data != null
         && (
           isStaffOrAdminOrTeacher()
-          || (data.participantIds is list && uid() in data.participantIds)
+          || isChatRoomParticipant(data)
           || (('parentUid' in data) && data.parentUid == uid())
           || (('studentAuthUid' in data) && data.studentAuthUid == uid())
+          || (('studentUid' in data) && data.studentUid == uid())
         );
     }
 
+    function canAccessChatRoomData(data) {
+      return canReadChatRoom(data);
+    }
+
     function canAccessChatRoomDoc() {
-      return canAccessChatRoomData(resource.data);
+      return canReadChatRoom(resource.data);
     }
 
 
     function announcementAudienceAuthUids() {
       return (
         resource != null
         && resource.data != null
         && ('audienceAuthUids' in resource.data)
         && resource.data.audienceAuthUids is list
       )
         ? resource.data.audienceAuthUids
         : [];
     }
 
     function canAccessChatRoomById(roomId) {
       return isSignedIn()
         && exists(/databases/$(database)/documents/chatRooms/$(roomId))
         && canAccessChatRoomData(get(/databases/$(database)/documents/chatRooms/$(roomId)).data);
     }
 
 
     function hasAllowedDirectChatCounterpart(participantIds) {
       return participantIds is list
         && (
           fixedTeacherAuthUid() in participantIds
@@ -730,88 +745,84 @@ service cloud.firestore {
       allow read: if signedIn()
         && (uid() == userUid || isStaffOrAdminOrTeacher());
 
       // ✅ 본인: 읽음 처리용 update만 허용
       allow update: if signedIn()
         && uid() == userUid
         && request.resource.data.diff(resource.data).changedKeys()
             .hasOnly(['isRead', 'readAt', 'updatedAt']);
 
       // ✅ 직원/관리자/강사: 기존대로 full 권한
       allow create, update, delete: if isStaffOrAdminOrTeacher();
     }
 
     match /notifications/{userUid}/meta {
       allow read, write: if signedIn()
         && (uid() == userUid || isStaffOrAdminOrTeacher());
     }
     
     match /notifications/{userUid}/meta/{metaId} {
       allow read, write: if signedIn()
         && (uid() == userUid || isStaffOrAdminOrTeacher());
     }
     
     
     match /chatRooms/{roomId} {
-      allow get, list: if canAccessChatRoomData(resource.data);
+      allow get, list: if canReadChatRoom(resource.data);
 
       allow create: if isSignedIn()
         && (
-          (
-            isStaffOrAdminOrTeacher()
-            && request.resource.data.type == 'individual'
-            && request.resource.data.participantIds is list
-            && request.resource.data.participantIds.size() == 2
-            && request.resource.data.internalOnly == true
-          )
+          isStaffOrAdminOrTeacher()
           || isValidParentDirectChatRoomCreate()
           || isValidStudentDirectChatRoomCreate()
         );
 
-      allow update: if isSignedIn() && (
-        isStaffOrAdminOrTeacher()
-        || (
-          canAccessChatRoomData(resource.data)
-          && isValidDirectChatRoomPatch()
-        )
-      );
+      allow update: if isSignedIn()
+        && (
+          isStaffOrAdminOrTeacher()
+          || (
+            canReadChatRoom(resource.data)
+            && isValidDirectChatRoomPatch()
+          )
+        );
 
       allow delete: if isStaffOrAdminOrTeacher();
     }
 
     match /chatRooms/{roomId}/messages/{messageId} {
       function roomDoc() {
         return get(/databases/$(database)/documents/chatRooms/$(roomId));
       }
 
-      function canAccessRoomMessages() {
-        return canAccessChatRoomData(roomDoc().data);
+      function canReadRoomMessages() {
+        return exists(/databases/$(database)/documents/chatRooms/$(roomId))
+          && canReadChatRoom(roomDoc().data);
       }
 
-      allow read: if canAccessRoomMessages();
+      allow read: if canReadRoomMessages();
 
-      allow create: if canAccessRoomMessages()
+      allow create: if canReadRoomMessages()
         && request.resource.data.senderId == uid()
         && request.resource.data.messageType in ['text', 'image', 'file']
         && request.resource.data.text is string
         && request.resource.data.text.size() <= 4000
         && isValidChatAttachmentList()
         && hasMessageTextOrAttachment()
         && request.resource.data.internalOnly == true;
 
       allow update: if isStaffOrAdminOrTeacher();
       allow delete: if false;
     }
 
     match /chatBroadcastLogs/{broadcastId} {
       allow read, write: if isStaffOrAdminOrTeacher();
     }
 
     match /chats/{roomId} {
       function isChatParticipant() {
         return isSignedIn()
           && resource.data.participantIds is list
           && uid() in resource.data.participantIds;
       }
 
       allow get, list: if isStaffOrAdminOrTeacher() || isChatParticipant();
 

diff --git a/src/components/StudentNotifications.jsx b/src/components/StudentNotifications.jsx
index e5150ff5be76a65d3e2b8f857dc4ec4880511c4d..5178f0363f0b69750972c5a5c4d45404232415f1 100644
--- a/src/components/StudentNotifications.jsx
+++ b/src/components/StudentNotifications.jsx
@@ -14,88 +14,88 @@ export default function StudentNotifications({ isOpen, onClose, notices = [], on
             }
         };
         if (isOpen) document.addEventListener('mousedown', handleClickOutside);
         return () => document.removeEventListener('mousedown', handleClickOutside);
     }, [isOpen, onClose]);
 
     const sortedNotices = [...notices].sort((a, b) => {
         if (a.isPinned !== b.isPinned) return b.isPinned - a.isPinned;
         return new Date(b.date) - new Date(a.date);
     });
 
     return (
         <div className={`fixed inset-0 z-[60] overflow-hidden pointer-events-none`}>
             <div 
                 className={`absolute inset-0 bg-black/30 transition-opacity duration-500 ease-in-out pointer-events-auto ${isOpen ? 'opacity-100' : 'opacity-0 pointer-events-none'}`} 
                 onClick={onClose}
             />
 
             <div 
                 ref={panelRef}
                 className={`absolute top-0 right-0 h-full w-full md:w-96 max-w-full bg-brand-bg shadow-2xl pointer-events-auto flex flex-col 
                 transform transition-transform duration-500 ease-[cubic-bezier(0.32,0.72,0,1)]
                 ${isOpen ? 'translate-x-0' : 'translate-x-full'}`}
             >
                 {/* 헤더 */}
-                <div className="bg-white px-5 py-4 flex justify-between items-center border-b border-brand-gray/30 shadow-sm shrink-0">
+                <div className="notification-mobile-header bg-white px-5 py-4 flex justify-between items-center border-b border-brand-gray/30 shadow-sm shrink-0">
                     <h3 className="font-bold text-brand-black text-lg flex items-center gap-2">
                         <div className="bg-brand-red/10 text-brand-red p-1.5 rounded-lg flex items-center justify-center">
                             <NotificationsIcon className="w-5 h-5" style={{ fontSize: 20 }} />
                         </div>
                         알림 센터
                     </h3>
-                    <button onClick={onClose} className="p-2 text-brand-gray hover:text-brand-black rounded-full hover:bg-brand-bg transition-colors">
+                    <button onClick={onClose} className="min-h-11 min-w-11 p-2 text-brand-gray hover:text-brand-black rounded-full hover:bg-brand-bg transition-colors flex items-center justify-center">
                         <Icon name="x" className="w-6 h-6" />
                     </button>
                 </div>
 
                 {/* 알림 리스트 */}
                 <div className="flex-1 overflow-y-auto p-4 space-y-3 bg-brand-bg custom-scrollbar">
                     {sortedNotices.length > 0 ? sortedNotices.map((notice) => (
                         <div 
                             key={notice.id} 
                             onClick={onNoticeClick}
                             className={`bg-white p-4 rounded-2xl shadow-sm border transition-all hover:shadow-md cursor-pointer group relative ${notice.isPinned ? 'border-brand-main/30 bg-brand-light/10' : 'border-brand-gray/20'}`}
                         >
                             <div className="flex justify-between items-start mb-2">
                                 <div className="flex items-center gap-2">
                                     {notice.isPinned && (
                                         <span className="bg-brand-red text-white text-[10px] font-bold px-1.5 py-0.5 rounded-md flex items-center gap-1">
                                             {/* ✅ [수정] 알림 리스트에서도 확성기 아이콘 사용 */}
                                             <CampaignIcon style={{ fontSize: 14 }} /> 필독
                                         </span>
                                     )}
                                     <span className="text-xs text-brand-gray font-medium bg-brand-bg px-2 py-0.5 rounded">
                                         {notice.author}
                                     </span>
                                 </div>
                                 <span className="text-xs text-brand-gray">{notice.date}</span>
                             </div>
                             <h4 className="font-bold text-brand-black mb-1 text-base">{notice.title}</h4>
                             <div 
                                 className="text-sm text-gray-700 line-clamp-2 leading-relaxed"
                                 dangerouslySetInnerHTML={{ __html: notice.content }}
                             />
                             
                             <button 
                                 onClick={(e) => {
                                     e.stopPropagation(); 
                                     onDelete(notice.id);
                                 }}
-                                className="absolute top-3 right-3 p-1.5 text-brand-gray hover:text-brand-red hover:bg-brand-red/10 rounded-full opacity-0 group-hover:opacity-100 transition-all"
+                                className="absolute top-3 right-3 min-h-11 min-w-11 p-1.5 text-brand-gray hover:text-brand-red hover:bg-brand-red/10 rounded-full opacity-0 group-hover:opacity-100 transition-all flex items-center justify-center"
                             >
                                 <Icon name="trash" className="w-4 h-4" />
                             </button>
                         </div>
                     )) : (
                         <div className="flex flex-col items-center justify-center h-64 text-brand-gray space-y-3">
                             <div className="bg-white p-4 rounded-full shadow-sm">
                                 <NotificationsIcon className="w-8 h-8 opacity-30" style={{ fontSize: 32 }} />
                             </div>
                             <p>새로운 알림이 없습니다.</p>
                         </div>
                     )}
                 </div>
             </div>
         </div>
     );
 }
\ No newline at end of file
diff --git a/src/index.css b/src/index.css
index b4fea6c6f1fa290aaf5313d7ccdca0beb8b5703b..95a2f0adf0674c7e902219ada13e932cfc2ff388 100644
--- a/src/index.css
+++ b/src/index.css
@@ -56,41 +56,47 @@ body {
   --app-safe-bottom: env(safe-area-inset-bottom, 0px);
 }
 
 body.capacitor-native,
 html.capacitor-native {
   background: #f8fafc;
 }
 
 .capacitor-native .mobile-screen {
   min-height: 100dvh;
 }
 
 .capacitor-native .student-mobile-header,
 .capacitor-native .parent-mobile-header,
 .capacitor-native .mobile-header {
   padding-top: max(var(--app-safe-top), 44px);
   min-height: calc(64px + max(var(--app-safe-top), 44px));
   height: auto;
   box-sizing: border-box;
 }
 
 .capacitor-native .mobile-header-content {
   min-height: 64px;
 }
 
+.capacitor-native .notification-mobile-header {
+  padding-top: max(var(--app-safe-top), 44px);
+  min-height: calc(64px + max(var(--app-safe-top), 44px));
+  box-sizing: border-box;
+}
+
 .back-button,
 .mobile-back-button {
   min-width: 44px;
   min-height: 44px;
   display: flex;
   align-items: center;
   justify-content: center;
   position: relative;
   z-index: 50;
   pointer-events: auto;
   flex-shrink: 0;
 }
 
 .capacitor-native .mobile-bottom-safe {
   padding-bottom: var(--app-safe-bottom);
 }
diff --git a/src/notifications/NotificationList.jsx b/src/notifications/NotificationList.jsx
index 0d54a6ae2f88b8ab858c84c430228186da2b6b77..598e4efcf384b3e6c04e1e65ab55af6c00eb8325 100644
--- a/src/notifications/NotificationList.jsx
+++ b/src/notifications/NotificationList.jsx
@@ -45,70 +45,70 @@ export default function NotificationList({
     return [...visibleNotifications].sort((a, b) => {
       const aTime = a.createdAt?.toMillis ? a.createdAt.toMillis() : 0;
       const bTime = b.createdAt?.toMillis ? b.createdAt.toMillis() : 0;
       return bTime - aTime;
     });
   }, [notifications, unreadOnly, lastReadAt]);
 
   // ✅ 렌더만 분기
   if (!isOpen) return null;
 
   return (
     <div className="fixed inset-0 z-[60] overflow-hidden">
       {/* 배경 */}
       <div
         className="absolute inset-0 bg-black/30"
         onClick={onClose}
         role="button"
         tabIndex={-1}
       />
 
       {/* 패널 */}
       <div
         ref={panelRef}
         className="absolute top-0 right-0 h-full w-full md:w-96 max-w-full bg-brand-bg shadow-2xl flex flex-col"
       >
-        <div className="bg-white px-5 py-4 flex justify-between items-center border-b border-brand-gray/30 shadow-sm shrink-0">
+        <div className="notification-mobile-header bg-white px-5 py-4 flex justify-between items-center border-b border-brand-gray/30 shadow-sm shrink-0">
           <h3 className="font-bold text-brand-black text-lg flex items-center gap-2">
             <div className="bg-brand-red/10 text-brand-red p-1.5 rounded-lg flex items-center justify-center">
               <NotificationsIcon className="w-5 h-5" style={{ fontSize: 20 }} />
             </div>
             알림 센터
           </h3>
           <div className="flex items-center gap-2">
             <button
               type="button"
               onClick={onMarkAllRead}
               disabled={!onMarkAllRead || unreadCount === 0}
-              className="text-xs font-semibold text-brand-main bg-brand-light/60 border border-brand-main/20 px-3 py-1 rounded-full disabled:opacity-50 disabled:cursor-not-allowed"
+              className="min-h-11 min-w-11 text-xs font-semibold text-brand-main bg-brand-light/60 border border-brand-main/20 px-3 py-1 rounded-full disabled:opacity-50 disabled:cursor-not-allowed"
             >
               모두 읽음
             </button>
             <button
               type="button"
               onClick={onClose}
-              className="p-2 text-brand-gray hover:text-brand-black rounded-full hover:bg-brand-bg transition-colors"
+              className="min-h-11 min-w-11 p-2 text-brand-gray hover:text-brand-black rounded-full hover:bg-brand-bg transition-colors flex items-center justify-center"
             >
               <Icon name="x" className="w-6 h-6" />
             </button>
           </div>
         </div>
 
         <div className="flex-1 overflow-y-auto p-4 space-y-3 bg-brand-bg custom-scrollbar">
           {isLoading ? (
             <div className="flex items-center justify-center h-32 text-sm text-brand-gray">
               알림을 불러오는 중...
             </div>
           ) : sortedNotifications.length > 0 ? (
             sortedNotifications.map((notification) => {
               const isUnread = isNotificationUnread(notification, lastReadAt);
 
               return (
                 <button
                   key={notification.id}
                   type="button"
                   onClick={() => onNotificationClick?.(notification)}
                   className={`w-full text-left bg-white p-4 rounded-2xl shadow-sm border transition-all hover:shadow-md group ${
                     isUnread ? 'border-brand-main/40 bg-brand-light/10' : 'border-brand-gray/20'
                   }`}
                 >
                   <div className="flex justify-between items-start mb-2">
